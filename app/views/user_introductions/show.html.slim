p#notice = notice

p = @user_introduction.id
p = @user_introduction.address
p = @user_introduction.image
p = @user_introduction.introduction

h3 ①位 
= @ranking1.rank
- if @ranking1.ranking_items[0].present?
  = @ranking1.ranking_items[0].shinto.name
  = @ranking1.ranking_items[0].memo
  = @ranking1.ranking_items[0].image
br
= link_to "追加する",  new_ranking_ranking_item_path(@ranking1.id)

h3 ②位 
= @ranking2.rank
- if @ranking2.ranking_items[0].present?
  = @ranking2.ranking_items[0].shinto.name
  = @ranking2.ranking_items[0].memo
  = @ranking2.ranking_items[0].image
br
= link_to "追加する",  new_ranking_ranking_item_path(@ranking2.id)
h3 ③位 
= @ranking3.rank
- if @ranking3.ranking_items[0].present?
  = @ranking3.ranking_items[0].shinto.name
  = @ranking3.ranking_items[0].memo
  = @ranking3.ranking_items[0].image
br
= link_to "追加する",  new_ranking_ranking_item_path(@ranking3.id)

br

= link_to 'Edit', edit_user_introduction_path(@user_introduction)
br
= link_to "Delete", user_introduction_path(@user_introduction.id), method: :delete

